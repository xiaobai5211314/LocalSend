name: localsend_integration
description: LocalSend enhanced integration module - unified entry point and integration tests
version: 1.0.0

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  crypto: ^3.0.3

dev_dependencies:
  test: ^1.24.0
