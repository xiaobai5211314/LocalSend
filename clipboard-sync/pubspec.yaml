name: clipboard_sync
description: LocalSend clipboard synchronization module with URL relay.
version: 0.1.0

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  web_socket_channel: ^2.4.0
  json_annotation: ^4.8.0
  json_serializable: ^6.7.0
  url_launcher: ^6.2.0
  crypto: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  build_runner: ^2.4.0
